//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use solana_pubkey::Pubkey;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const UPDATE_TREASURY_AUTHORITY_DISCRIMINATOR: [u8; 8] = [185, 149, 66, 195, 105, 183, 32, 244];

/// Accounts.
#[derive(Debug)]
pub struct UpdateTreasuryAuthority {
      
              
          pub current_authority: solana_pubkey::Pubkey,
          
              
          pub treasury: solana_pubkey::Pubkey,
          
              
          pub a_mint: solana_pubkey::Pubkey,
      }

impl UpdateTreasuryAuthority {
  pub fn instruction(&self, args: UpdateTreasuryAuthorityInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: UpdateTreasuryAuthorityInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.current_authority,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.treasury,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.a_mint,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = UpdateTreasuryAuthorityInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::CPMM_POC_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct UpdateTreasuryAuthorityInstructionData {
            discriminator: [u8; 8],
            }

impl UpdateTreasuryAuthorityInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [185, 149, 66, 195, 105, 183, 32, 244],
                                }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for UpdateTreasuryAuthorityInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct UpdateTreasuryAuthorityInstructionArgs {
                  pub new_treasury_authority: Pubkey,
      }

impl UpdateTreasuryAuthorityInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `UpdateTreasuryAuthority`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` current_authority
                ///   1. `[writable]` treasury
          ///   2. `[]` a_mint
#[derive(Clone, Debug, Default)]
pub struct UpdateTreasuryAuthorityBuilder {
            current_authority: Option<solana_pubkey::Pubkey>,
                treasury: Option<solana_pubkey::Pubkey>,
                a_mint: Option<solana_pubkey::Pubkey>,
                        new_treasury_authority: Option<Pubkey>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateTreasuryAuthorityBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            #[inline(always)]
    pub fn current_authority(&mut self, current_authority: solana_pubkey::Pubkey) -> &mut Self {
                        self.current_authority = Some(current_authority);
                    self
    }
            #[inline(always)]
    pub fn treasury(&mut self, treasury: solana_pubkey::Pubkey) -> &mut Self {
                        self.treasury = Some(treasury);
                    self
    }
            #[inline(always)]
    pub fn a_mint(&mut self, a_mint: solana_pubkey::Pubkey) -> &mut Self {
                        self.a_mint = Some(a_mint);
                    self
    }
                    #[inline(always)]
      pub fn new_treasury_authority(&mut self, new_treasury_authority: Pubkey) -> &mut Self {
        self.new_treasury_authority = Some(new_treasury_authority);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = UpdateTreasuryAuthority {
                              current_authority: self.current_authority.expect("current_authority is not set"),
                                        treasury: self.treasury.expect("treasury is not set"),
                                        a_mint: self.a_mint.expect("a_mint is not set"),
                      };
          let args = UpdateTreasuryAuthorityInstructionArgs {
                                                              new_treasury_authority: self.new_treasury_authority.clone().expect("new_treasury_authority is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `update_treasury_authority` CPI accounts.
  pub struct UpdateTreasuryAuthorityCpiAccounts<'a, 'b> {
          
                    
              pub current_authority: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub treasury: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub a_mint: &'b solana_account_info::AccountInfo<'a>,
            }

/// `update_treasury_authority` CPI instruction.
pub struct UpdateTreasuryAuthorityCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
      
              
          pub current_authority: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub treasury: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub a_mint: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: UpdateTreasuryAuthorityInstructionArgs,
  }

impl<'a, 'b> UpdateTreasuryAuthorityCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: UpdateTreasuryAuthorityCpiAccounts<'a, 'b>,
              args: UpdateTreasuryAuthorityInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              current_authority: accounts.current_authority,
              treasury: accounts.treasury,
              a_mint: accounts.a_mint,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.current_authority.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.treasury.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.a_mint.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = UpdateTreasuryAuthorityInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::CPMM_POC_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.current_authority.clone());
                        account_infos.push(self.treasury.clone());
                        account_infos.push(self.a_mint.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `UpdateTreasuryAuthority` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` current_authority
                ///   1. `[writable]` treasury
          ///   2. `[]` a_mint
#[derive(Clone, Debug)]
pub struct UpdateTreasuryAuthorityCpiBuilder<'a, 'b> {
  instruction: Box<UpdateTreasuryAuthorityCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateTreasuryAuthorityCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(UpdateTreasuryAuthorityCpiBuilderInstruction {
      __program: program,
              current_authority: None,
              treasury: None,
              a_mint: None,
                                            new_treasury_authority: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      #[inline(always)]
    pub fn current_authority(&mut self, current_authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.current_authority = Some(current_authority);
                    self
    }
      #[inline(always)]
    pub fn treasury(&mut self, treasury: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.treasury = Some(treasury);
                    self
    }
      #[inline(always)]
    pub fn a_mint(&mut self, a_mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.a_mint = Some(a_mint);
                    self
    }
                    #[inline(always)]
      pub fn new_treasury_authority(&mut self, new_treasury_authority: Pubkey) -> &mut Self {
        self.instruction.new_treasury_authority = Some(new_treasury_authority);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = UpdateTreasuryAuthorityInstructionArgs {
                                                              new_treasury_authority: self.instruction.new_treasury_authority.clone().expect("new_treasury_authority is not set"),
                                    };
        let instruction = UpdateTreasuryAuthorityCpi {
        __program: self.instruction.__program,
                  
          current_authority: self.instruction.current_authority.expect("current_authority is not set"),
                  
          treasury: self.instruction.treasury.expect("treasury is not set"),
                  
          a_mint: self.instruction.a_mint.expect("a_mint is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct UpdateTreasuryAuthorityCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            current_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
                treasury: Option<&'b solana_account_info::AccountInfo<'a>>,
                a_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
                        new_treasury_authority: Option<Pubkey>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

